1. Make sure to reset the page so the message sent doesn't still show in the select to review box after submission.
2. Fix how the error messages pop up
3. The Entire UI is still crap so we need to use a different development library
4. Clicking connect stripe right after connecting still redirects to the stripes connect page. I'd like it to show stripe already connected toast
5. Download a UI library.
6. Change fonts